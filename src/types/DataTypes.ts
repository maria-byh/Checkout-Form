export type ProductType = {
    id: string;  
    itemsNumber: number;         
    name: string;         
    description: string;  
    taxRate: number;
    price: number;       
    image: string;        
};
